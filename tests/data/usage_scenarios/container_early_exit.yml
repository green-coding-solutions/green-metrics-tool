---
name: Test Stress
author: Dan Mateas
description: test

services:
  test-container:
    image: alpine
    entrypoint: sleep 5

  test-container-2:
    image: alpine
    entrypoint: sleep 2


flow:
  - name: Phase 1
    container: test-container
    commands:
        - type: console
          command: sleep 2

  - name: Phase 2 should fail
    container: test-container
    commands:
      - type: console
        command: echo "asd"

