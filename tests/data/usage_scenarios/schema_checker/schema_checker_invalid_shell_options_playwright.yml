---
name: Test
author: Dan Mateas
description: test

services:
  test-container:
    image: gcb_stress
    build: .

flow:
  - name: Stress
    container: test-container
    commands:
      - type: playwright
        command: page.goto('https://www.green-coding.io')
        shell: bash
        shell-options: ['-o', 'errexit']
