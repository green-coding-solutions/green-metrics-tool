---
name: Schema Checker Valid Host Execution
author: Didi Hoffmann
description: Host execution flows with container as YAML null

flow:
  - name: Host flow with YAML null
    container: null
    commands:
      - type: console
        command: echo 1

  - name: Host flow with empty container value
    container:
    commands:
      - type: console
        command: echo 2
