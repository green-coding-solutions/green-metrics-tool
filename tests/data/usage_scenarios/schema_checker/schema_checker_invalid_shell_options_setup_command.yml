---
name: Test
author: Dan Mateas
description: test

services:
  test-container:
    image: gcb_stress
    build: .
    setup-commands:
      - command: ps -a
        shell-options: ['-o', 'errexit']

flow:
  - name: Stress
    container: test-container
    commands:
      - type: console
        command: stress-ng -c 1 -t 1 -q
        note: Starting Stress
