---
name: Test
author: Dan Mateas
description: test

isolation:
  type: vm
  backend: kata-qemu

services:
  test-container:
    image: alpine:latest

flow:
  - name: Stress
    container: test-container
    commands:
      - type: console
        command: stress-ng -c 1 -t 1 -q
        note: Starting Stress
