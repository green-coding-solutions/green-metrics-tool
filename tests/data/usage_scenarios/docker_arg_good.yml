---
name: Container name invalid test
author: Arne Tarara
description: Container name invalid test

services:
  test:
    image: alpine
    container_name: test
    docker-run-args:
      - --label test=true

flow:
  - name: Small-Stress
    container: test
    commands:
      - type: console
        command: echo "asd"
        shell: /bin/sh
