---
name: Testing Noop
author: Arne Tarara
description: Testing Noop

services:
  test-container:
    image: alpine

flow:
  - name: Testing Noop
    container: test-container
    commands:
      - type: console
        command: echo 1
